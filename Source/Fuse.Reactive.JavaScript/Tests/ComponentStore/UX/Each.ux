<Panel ux:Class="UX.Model.Each" Model="UX/ModelEach">
	<Panel ux:Name="a">
		<Each Items="{simple}">
			<FuseTest.DudElement StringValue="{}"/>
		</Each>
	</Panel>
	
	<Panel ux:Name="b">
		<Each Items="{items}">
			<FuseTest.DudElement StringValue="{name}"/>
		</Each>
	</Panel>
</Panel>