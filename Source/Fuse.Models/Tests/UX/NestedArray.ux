<Panel ux:Class="UX.Model.NestedArray" Model="UX/NestedArray">
	<Each Items="{array}">
		<Each Items="data()">
			<Text Value="{=data()}" />
		</Each>
	</Each>
	<FuseTest.Invoke ux:Name="push" Handler="{push}" />
</Panel>
